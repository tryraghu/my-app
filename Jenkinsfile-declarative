pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps{
                git 'https://github.com/tryraghu/my-app.git'
            }
        stage("Maven Build"){
            steps{
                sh "mvn clean package"
            }
        }
    }
    
}
